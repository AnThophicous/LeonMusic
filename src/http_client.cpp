#include "internal.hpp"

namespace leonmusic {
// HTTP integration will sit behind service adapters. The current scaffold keeps CURL optional
// so the core library compiles cleanly on fresh Windows workstations and CI images.
}

